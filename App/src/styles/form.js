import { StyleSheet } from "react-native";

export const form = StyleSheet.create({
    
    input: {
        marginBottom: 20,
    },
    buttonSubmit: {
        padding: 5
    },
    buttonText: {
        marginTop: 10,
    }
});